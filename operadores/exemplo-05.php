<?php
    // operador Spaceship (somente no php7)
    // Retorna um integer menor que, igual a ou maior que zero quando $a é, respectivamente, menor que, igual a ou maior que $b.
    $a = 50;
    $b = 35;
    var_dump($a <=> $b);
?>